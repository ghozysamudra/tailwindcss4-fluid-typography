---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["rounded-2xl border border-[color:var(--line)] bg-[color:var(--surface)]", className]}>
  <slot />
</div>
