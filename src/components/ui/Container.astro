---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...rest } = Astro.props;
---

<div
  class:list={["mx-auto w-[min(1920px,calc(100%-2rem))] max-[760px]:w-[min(1100px,calc(100%-1.2rem))]", className]}
  {...rest}
>
  <slot />
</div>
