<section class="section-pad">
  <div class="container-shell reveal">
    <header class="card grid gap-8 overflow-hidden p-6 md:grid-cols-[1.2fr_0.8fr] md:p-10">
      <div>
        <p class="text-overline m-0 text-[color:var(--accent)]">Tailwind CSS v4 / Fluid Type</p>
        <h1 class="text-display-xl mt-3 max-w-3xl">
          Typography That Adapts to Every Screen
        </h1>
        <p class="text-body-xl mt-4 max-w-2xl text-[color:var(--text-muted)]">
          Build beautiful, responsive websites with a fluid type system that scales seamlessly from
          mobile to desktop with zero media queries.
        </p>
        <div class="mt-8 flex flex-wrap gap-3">
          <a href="#getting-started" class="btn btn-primary text-body">Get Started</a>
          <a href="#type-scale" class="btn btn-secondary text-body">View Demo</a>
          <button type="button" class="btn btn-secondary text-body" data-theme-toggle aria-pressed="false">
            Switch to Dark
          </button>
        </div>
      </div>

      <div class="card p-5">
        <p class="text-h6 mono text-[color:var(--text-muted)]">Live Snapshot</p>
        <div class="mt-3 space-y-3">
          <p class="text-display m-0">Display text</p>
          <p class="text-h2 m-0">Headline rhythm</p>
          <p class="text-body m-0 text-[color:var(--text-muted)]">
            Resize the window and every line scales in proportion.
          </p>
          <div class="mt-3 h-2 rounded-full bg-[color:var(--bg-muted)]">
            <div class="h-2 w-3/4 rounded-full bg-[color:var(--accent)]"></div>
          </div>
        </div>
      </div>
    </header>
  </div>
</section>
